interface:
  display_name: "Expert Roundtable"
  short_description: "真实专家圆桌：自动选专家，必要时多 agent，输出形态由 agent 决定"
  default_prompt: "Use $ljg-roundtable to select real domain experts, optionally run gpt-5.4 high subagents, avoid role-play, and synthesize the topic in the best structure for the task."
